/**
 * Write the corresponding C mathematical expressions.
 * Author: Nicolas Ventura
 */

int main(void) {
    double c, f, i, r, x, y;
    /* Part (c) */
    c = (4.0 / 3) * 3.14 * r * r * r;
    /* Part (f) */
    f = (x >= y && x >= 0);
    /* Part (i) */
    i = (1 < x && x < 20) || (3 >= y && y >= 1);
    return 0;
}